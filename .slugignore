# Exclude everything except API-related files
# This dramatically reduces slug size for Heroku deployment

# Exclude entire applications that aren't needed
apps/web/
apps/docs/

# Exclude packages not needed at runtime
packages/libs/
packages/typescript-config/

# Exclude email package source (dist is committed to git)
packages/email/src/
packages/email/tsconfig.json

# Exclude development files and docs
*.md
CHANGELOG.md
CONTRIBUTING.md
CONTRIBUTORS.svg
ENVIRONMENT_SETUP.md
LICENSE
README.md

# Exclude config files not needed at runtime
.git/
.github/
.husky/
.vscode/
charts/
compose.local.yml
compose.yml
commitlint.config.js
biome.json
turbo.json
sync-web-repo.sh
.buildpacks

# Exclude some API development files (but keep src/ for build)
apps/api/HEROKU_DEPLOYMENT.md
apps/api/app.json
apps/api/Dockerfile
apps/api/.slugignore

# Keep email package (needed at runtime)
packages/email/tsconfig.json

